#!/usr/bin/env bash
# lifted from omarchy/bin/omarchy-launch-tui

# Launch the TUI command passed in as an argument in the default terminal with a dot.tui.COMMAND app id for styling.

exec setsid uwsm-app -- xdg-terminal-exec --app-id=dot.tui."$(basename "$1")" -e "$1" "${@:2}"
